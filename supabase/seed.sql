-- =============================================================================
-- 시드 데이터: 데모용 대학 목록
-- =============================================================================
INSERT INTO universities (slug, name, code, active) VALUES
  ('snu', '서울대학교', 'SNU-001', TRUE),
  ('yonsei', '연세대학교', 'YS-001', TRUE),
  ('korea', '고려대학교', 'KU-001', TRUE),
  ('kaist', 'KAIST', 'KAIST-001', TRUE)
ON CONFLICT (slug) DO NOTHING;

-- =============================================================================
-- 초기 관리자 승격 (이메일로 해당 사용자 찾아 admin 지정)
-- 실사용시: Supabase Dashboard에서 계정 생성 후 이 쿼리의 email만 변경하여 실행
-- =============================================================================
-- UPDATE profiles 
-- SET role = 'admin', name = '시스템 관리자'
-- WHERE email = 'admin@your-domain.com';
